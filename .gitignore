# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# --- sports-analytics ---
/target
/src-tauri/target
/src-tauri/gen/schemas
# Models are large binaries regenerated by scripts/export-models.py, and
# their licences differ from this repo's — manifest.toml records what they
# should be, which is what actually needs versioning.
/models/*.onnx
# The ONNX Runtime shared library, installed per-machine by
# scripts/setup-runtime.sh: it is platform-specific and ~15-300 MB.
/runtime/
/outputs/
/samples/
*.zip
